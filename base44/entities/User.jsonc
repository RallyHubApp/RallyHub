{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "display_name": {
      "type": "string",
      "description": "Custom display name set by admin (overrides the auto-generated name from email)"
    }
  },
  "required": [
    "role"
  ]
}