{
  "name": "Court",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Court name or number"
    },
    "location": {
      "type": "string"
    },
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional public description of the court"
    },
    "status": {
      "type": "string",
      "enum": [
        "Available",
        "In Use",
        "Maintenance"
      ],
      "default": "Available"
    },
    "surface_type": {
      "type": "string",
      "enum": [
        "Indoor",
        "Outdoor - Hard",
        "Outdoor - Clay",
        "Outdoor - Grass"
      ]
    },
    "current_match_id": {
      "type": "string"
    },
    "tenant_id": {
      "type": "string",
      "description": "Owning Tenant ID"
    },
    "club_id": {
      "type": "string",
      "description": "Owning Club ID"
    },
    "venue_id": {
      "type": "string",
      "description": "Owning Venue ID"
    }
  },
  "required": [
    "name"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": true,
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}